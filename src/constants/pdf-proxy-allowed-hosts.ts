/** Hostnames permitted by `/api/campaign-brief-image` and client-side PDF image URLs. */
export const PDF_PROXY_ALLOWED_IMAGE_HOSTS = new Set([
  'ishout.s3.us-east-2.amazonaws.com',
  'ishout.s3.amazonaws.com',
]);
